# ClearGrid

Multi-agent waste orchestration platform — transforms static municipal garbage collection into a dynamic, proactive digital grid.

## Quick Start

### Backend

```bash
cd backend
pip install -r requirements.txt
uvicorn main:app --reload
```

### Frontend

```bash
cd frontend
npm install
npm run dev
```

## Architecture

See `00_MASTER_ARCHITECTURE.md` for full details.
